<div class="col-xl-12 grid-margin" style="margin-left: 0;padding-left: 0;margin-right: 0;padding-right: 0;">
  <div class="card">
    <div class="card-body">
      <div class="row">
        <div class="col-xl-6">
          <div class="wrapper">
            <h3 class="mb-0 font-weight-semibold">
              <%if(cclogins[0].count != -1){%>
                <%= cclogins[0].count %>
              <%}%> 
            </h3>
            <h5 class="mb-0 font-weight-medium text-primary">Login's(Hoje)</h5>
          </div>
        </div>
        <div class="col-xl-6">
          <div class="wrapper">
            <h3 class="mb-0 font-weight-semibold">
              <%if(clogins[0].count != -1){%>
                <%= clogins[0].count %>
              <%}%>  
            </h3>
            <h5 class="mb-0 font-weight-medium text-primary">Login's(Sempre)</h5>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
